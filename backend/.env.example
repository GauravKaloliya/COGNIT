# =====================================================
# C.O.G.N.I.T. Backend Environment Variables (Template)
# =====================================================
# Copy to .env, .env.development, or .env.production and fill required values.

# --- Core App ---
WEBSITE_URL=http://localhost:5000

# Cookies (optional)
# SESSION_COOKIE_SECURE defaults based on WEBSITE_URL (https -> true)
SESSION_COOKIE_SECURE=
# SESSION_COOKIE_SAMESITE defaults based on SESSION_COOKIE_SECURE (https -> None, else Lax)
SESSION_COOKIE_SAMESITE=

# --- Content Validation ---
MIN_WORD_COUNT=60
MIN_DESCRIPTION_LENGTH=60
MAX_DESCRIPTION_LENGTH=10000
MIN_FEEDBACK_LENGTH=5
MAX_FEEDBACK_LENGTH=2000
MIN_RATING=1
MAX_RATING=10
TOO_FAST_SECONDS=5.0

# --- UPI / Payment ---
UPI_NAME=
# Comma-separated list of VPAs (required)
UPI_POOL_VPAS=
PAYMENT_AMOUNT=1
PAYMENT_SECRET=
PAYMENT_EXPIRY_SECONDS=300
PAYMENT_SCREENSHOT_TIMEZONE=Asia/Kolkata

# UPI pool allocator (optional; defaults shown)
UPI_PER_UPI_LIMIT=20
UPI_USER_LIMIT=6
UPI_COOLDOWN_SECONDS=600
UPI_FAILURE_DECAY_SECONDS=600
UPI_BURST_RESET_SECONDS=60
UPI_COOLDOWN_CAPACITY_FACTOR=0.5
UPI_TIE_SCORE_THRESHOLD=1

EMAIL_OTP_LENGTH=6
EMAIL_OTP_EXPIRY_SECONDS=300
EMAIL_OTP_MAX_ATTEMPTS=5
EMAIL_OTP_WEBHOOK_URL=
EMAIL_OTP_SENDER=verify@cognit.online
EMAIL_OTP_SUBJECT=Verification COGNIT
EMAIL_OTP_HTML_TEMPLATE=<p><strong>COGNIT</strong></p><p>Enter this verification code to continue:</p><p style="font-size:20px;font-weight:600;letter-spacing:2px;">{otp}</p><p>Please ignore this email if this wasn't you trying to create a COGNIT participant account.</p><p>Best,<br>The COGNIT team<br>COGNIT</p>
EMAIL_OTP_WEBHOOK_TIMEOUT_SECONDS=15
EMAIL_OTP_JWT_SECRET=
EMAIL_OTP_JWT_TTL_SECONDS=3600

# --- Turnstile / Bot Defense ---
TURNSTILE_ENABLED=false
TURNSTILE_SECRET_KEY=
TURNSTILE_VERIFY_URL=https://challenges.cloudflare.com/turnstile/v0/siteverify
TURNSTILE_TIMEOUT_SECONDS=3.5
TURNSTILE_BYPASS_LOCAL=true

# --- OCR / Fraud ---
MIN_OCR_CONFIDENCE=55
MIN_IMAGE_WIDTH=600
IMAGE_VALIDATE_URL_AVAILABILITY=false
ALLOWED_IMAGE_EXTENSIONS=jpg,jpeg,png,webp
MAX_FRAUD_SCORE=50.0
ENABLE_DUPLICATE_DETECTION=true
ENABLE_DEVICE_FINGERPRINTING=true
ENABLE_AUDIT_LOGGING=true
ENABLE_ERROR_LOGGING=true
SUPPRESS_ACCESS_LOGS=true
DEVICE_FINGERPRINT_SALTS=cognit_fingerprint_salt_2026
FORCE_ATTENTION_IMAGES=false

# --- AWS / S3 ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
S3_BUCKET_NAME=cognitapi

# --- Security ---
IP_HASH_SALT=
SECRET_KEY=

# --- Database ---
DATABASE_URL=
DATABASE_SSLMODE=auto

# --- CORS / Rate Limiting ---
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
CORS_SUPPORTS_CREDENTIALS=true
TRUST_PROXY_HEADERS=true
RATELIMIT_STORAGE_URI=memory://
CACHE_REDIS_URL=
CACHE_REDIS_PREFIX=cognit
PAYMENT_STATUS_CACHE_TTL_SECONDS_ACTIVE=1
PAYMENT_STATUS_CACHE_TTL_SECONDS_TERMINAL=15
PARTICIPANT_OPTIONS_CACHE_TTL_SECONDS=300
DOCS_BASE_URL=http://localhost:5000

# --- Image Pool ---
IMAGE_PICK_ATTEMPTS_ATTENTION=4
IMAGE_PICK_ATTEMPTS_NON_ATTENTION=4

# --- Runtime (optional) ---
PORT=5000
FLASK_DEBUG=true
PAYMENT_MAX_IMAGE_MB=8
