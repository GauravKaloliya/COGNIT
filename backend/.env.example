# =====================================================
# C.O.G.N.I.T. Backend Environment Variables
# =====================================================

# Database Configuration
# PostgreSQL connection string - Required
# Format: postgresql://username:password@host:port/database
DATABASE_URL=

# Flask Configuration
# Required - use a long random string in production
SECRET_KEY=

# Website URL
# Used for API documentation base URL
WEBSITE_URL=

# CORS Configuration
# Comma-separated list of allowed origins
# Example: https://cognit.online,https://www.cognit.online
CORS_ORIGINS=

# Application Configuration
MIN_WORD_COUNT=60
MIN_DESCRIPTION_LENGTH=60
MAX_DESCRIPTION_LENGTH=10000
MIN_FEEDBACK_LENGTH=5
MAX_FEEDBACK_LENGTH=2000
MIN_RATING=1
MAX_RATING=10
TOO_FAST_SECONDS=5.0

# Attention & Quality Configuration
ATTENTION_FLAG_THRESHOLD=0.60
ATTENTION_FLAG_MIN_CHECKS=3
PRIORITY_WORD_THRESHOLD=500
PRIORITY_ROUNDS_THRESHOLD=3
PRIORITY_ATTENTION_THRESHOLD=0.75

# Performance logging sample rate (0.0 to 1.0, default: 0.10 = 10%)
PERFORMANCE_LOG_SAMPLE_RATE=0.10

# IP anonymization salt - Required, use a long random string
IP_HASH_SALT=

# Rate Limiting Storage Backend
# Use memory:// for single-server deployments
# For distributed deployments, use Redis: redis://host:port/db
RATELIMIT_STORAGE_URI=memory://

# =====================================================
# Payment & UPI Configuration
# =====================================================

# UPI Virtual Payment Address (VPA)
# Example: yourbusiness@upi
UPI_VPA=

# Your business/personal name for UPI payments
UPI_NAME=

# Payment signature secret (use a long random string)
# Generate with: openssl rand -hex 32
PAYMENT_SECRET=

# Payment expiry time in seconds (default: 300 = 5 minutes)
PAYMENT_EXPIRY_SECONDS=300

# Payment amount in INR (default: 1)
PAYMENT_AMOUNT=1

# =====================================================
# AWS S3 Configuration
# =====================================================

# AWS Access Key ID
AWS_ACCESS_KEY_ID=

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=

# AWS Region (e.g., ap-south-1, us-east-1)
AWS_REGION=ap-south-1

# S3 Bucket name for storing payment proof files
S3_BUCKET=cognitapi

# =====================================================
# Survey Configuration
# =====================================================

# Number of survey rounds per participant
SURVEY_ROUNDS=1

# Minimum submissions for priority eligibility
PRIORITY_MIN_SUBMISSIONS=3

# =====================================================
# Gunicorn Configuration (for Docker/Container deployment)
# =====================================================

# Gunicorn server binding address (default: 0.0.0.0:5000)
# GUNICORN_BIND=0.0.0.0:5000

# Number of worker processes (default: auto-calculated based on CPU cores)
# GUNICORN_WORKERS=2

# Number of threads per worker (default: 4)
# GUNICORN_THREADS=4

# Worker class: 'sync' (default) or 'gevent' for async
# GUNICORN_WORKER_CLASS=sync

# Worker silent timeout in seconds (default: 60)
# GUNICORN_TIMEOUT=60

# Keep connections alive for this many seconds (default: 5)
# GUNICORN_KEEPALIVE=5

# Restart workers after this many requests (default: 1000)
# GUNICORN_MAX_REQUESTS=1000

# Random jitter for max_requests (default: 50)
# GUNICORN_MAX_REQUESTS_JITTER=50

# Log level: debug, info, warning, error, critical (default: info)
# GUNICORN_LOG_LEVEL=info
