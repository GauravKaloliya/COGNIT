[1mdiff --git a/backend/schema.sql b/backend/schema.sql[m
[1mindex 5ef936a..79825a2 100644[m
[1m--- a/backend/schema.sql[m
[1m+++ b/backend/schema.sql[m
[36m@@ -19,6 +19,20 @@[m [mCREATE TABLE IF NOT EXISTS participants ([m
     place VARCHAR(100),[m
     native_language VARCHAR(50),[m
     prior_experience VARCHAR(100),[m
[32m+[m[32m    -- FIX: CHECK constraint for gender[m
[32m+[m[32m    CONSTRAINT valid_gender CHECK ([m
[32m+[m[32m        gender IS NULL OR[m
[32m+[m[32m        gender IN ('male', 'female', 'other', 'prefer_not_to_say')[m
[32m+[m[32m    ),[m
[32m+[m[32m    -- FIX: CHECK constraint for native_language (common languages)[m
[32m+[m[32m    CONSTRAINT valid_native_language CHECK ([m
[32m+[m[32m        native_language IS NULL OR[m
[32m+[m[32m        native_language IN ([m
[32m+[m[32m            'english', 'hindi', 'spanish', 'french', 'german', 'chinese', 'japanese', 'korean',[m
[32m+[m[32m            'arabic', 'portuguese', 'russian', 'italian', 'dutch', 'turkish', 'polish',[m
[32m+[m[32m            'other'[m
[32m+[m[32m        )[m
[32m+[m[32m    ),[m
     consent_given BOOLEAN DEFAULT FALSE,[m
     consent_timestamp TIMESTAMPTZ,[m
     payment_status VARCHAR(50) DEFAULT 'pending',[m
[36m@@ -135,7 +149,7 @@[m [mCREATE TABLE IF NOT EXISTS submissions ([m
     session_id VARCHAR(100) NOT NULL,[m
     image_id VARCHAR(100) NOT NULL,[m
     image_url VARCHAR(500),[m
[31m-    survey_index INTEGER NOT NULL,[m
[32m+[m[32m    survey_index INTEGER,  -- FIX: Allow NULL for non-survey submissions[m
     description TEXT NOT NULL CHECK (length(description) BETWEEN 10 AND 10000),[m
     word_count INTEGER NOT NULL CHECK (word_count BETWEEN 0 AND 10000),[m
     rating INTEGER NOT NULL CHECK (rating BETWEEN 1 AND 10),[m
[36m@@ -161,8 +175,7 @@[m [mCREATE TABLE IF NOT EXISTS submissions ([m
         FOREIGN KEY (image_id)[m
         REFERENCES images(image_id),[m
 [m
[31m-    CONSTRAINT unique_participant_survey_index[m
[31m-        UNIQUE (participant_fk, survey_index),[m
[32m+[m[32m    -- FIX: Partial unique index created separately below instead of this constraint[m
 [m
     CONSTRAINT submissions_attention_score_range[m
         CHECK (attention_score_at_submission IS NULL OR attention_score_at_submission BETWEEN 0 AND 1),[m
[36m@@ -305,7 +318,14 @@[m [mCREATE INDEX IF NOT EXISTS idx_submissions_survey ON submissions(is_survey);[m
 CREATE INDEX IF NOT EXISTS idx_submissions_attention ON submissions(is_attention);[m
 CREATE INDEX IF NOT EXISTS idx_submissions_quality ON submissions(quality_score);[m
 CREATE INDEX IF NOT EXISTS idx_submissions_ai_suspected ON submissions(ai_suspected);[m
[32m+[m[32m-- FIX: Partial unique index for survey_index - only applies when survey_index IS NOT NULL[m
[32m+[m[32mCREATE UNIQUE INDEX IF NOT EXISTS unique_survey_index[m
[32m+[m[32mON submissions(participant_fk, survey_index)[m
[32m+[m[32mWHERE survey_index IS NOT NULL;[m
 CREATE INDEX IF NOT EXISTS idx_submissions_survey_index ON submissions(participant_fk, survey_index);[m
[32m+[m[32m-- FIX: Composite index for analytics queries (participant submissions sorted by time)[m
[32m+[m[32mCREATE INDEX IF NOT EXISTS idx_submissions_participant_created[m
[32m+[m[32mON submissions(participant_fk, created_at DESC);[m
 [m
 CREATE INDEX IF NOT EXISTS idx_consent_participant_fk ON consent_records(participant_fk);[m
 CREATE INDEX IF NOT EXISTS idx_consent_participant_id ON consent_records(participant_id);[m
