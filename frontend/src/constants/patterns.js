export const REGEX_PATTERNS = {
  username: /^[a-zA-Z0-9_]+$/,
  email: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
  digitsOnly: /^\d+$/,
  uuidTemplateToken: /[xy]/g,
};

export const STORAGE_EVENTS = {
  storage: "storage",
};
